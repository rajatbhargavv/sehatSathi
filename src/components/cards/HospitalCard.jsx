import React from 'react';
import Card from '../ui/Card';

const HospitalCard = ({ data }) => {
  return <Card className="hospitalcard">{/* render HospitalCard data */}</Card>;
};

export default HospitalCard;
