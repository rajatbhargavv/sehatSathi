import React from 'react';
import Card from '../ui/Card';

const DoctorCard = ({ data }) => {
  return <Card className="doctorcard">{/* render DoctorCard data */}</Card>;
};

export default DoctorCard;
